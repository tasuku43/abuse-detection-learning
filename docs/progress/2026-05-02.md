# 2026-05-02

## やったこと

* README を人間向けの入口として整備した
* AGENTS.md に coding agent 向けの作業方針を追加した
* roadmap / progress log / task list の方針を決めた
* progress log を日別ファイルで管理する方針にした
* task list を `docs/tasks.md` に置く方針にした
* `.gitignore` を追加した

## 学んだこと

* このリポジトリでは、feature generation、scoring function、evaluation harness を分けて考える
* 最初は実 DB や ML framework ではなく、合成 CSV とルールベース scoring_fn で評価の流れを理解する
* progress を日別ファイルにすると、「昨日やったこと」などをピンポイントに読みやすい
* ルート直下は入口のドキュメントに絞り、進捗管理系は `docs/` に寄せると見通しがよい

## 次にやること

* Phase 1 の最小 evaluation harness を実装する
* `pytest -q` が通る最小構成を作る

## 疑問や保留

* 最初の scoring rule をどの程度単純にするか
* notebook を Phase 1 と同時に作るか、評価コードが固まってから作るか
