# 2026-05-06

## やったこと

* `docs/design/evaluation_harness_architecture_mermaid_ja.md` を読み、既存 Phase 0〜7 が評価基盤設計の理解に対応していることを確認した
* `docs/design/production_scoring_architecture_mermaid_ja.md` を読み、次の学習対象が ScoreResult、Decision Policy、ActionCandidate、Review Queue、Dry-run Action Worker であることを整理した
* `docs/design/` の具体的な業務名詞を、public repo に置ける架空設計の抽象表現へ寄せた
* `docs/design/` の Mermaid 図に、データストア形状、判断ノード、処理ノード、観察ノード、色分けを追加した
* `docs/design/` から `Learning Project で試す範囲` や `推奨する次の実装タスク` を外し、ロードマップではなく純粋な設計書として読めるようにした
* `docs/planning/roadmap.md` を、評価基盤中心のロードマップから `docs/design` 理解用ロードマップへ組み直した
* `README.md` の位置づけを、評価基盤単体ではなく SaaS 不正検知アーキテクチャ理解用リポジトリとして更新した
* `AGENTS.md` の冒頭を更新し、次回以降の作業が Phase 8 以降へ進むようにした
* `docs/planning/tasks.md` に Phase 8〜13 の新しいタスクを追加した
* `docs/` を `design/`、`planning/`、`process/`、`learning/`、`progress/` に整理した

## 学んだこと

* 既存の評価基盤は、本番運用そのものではなく、scorer を育てるための土台である
* 本番スコアリング運用では、scorer は直接 action せず、ScoreResult を出すだけに保つ
* action するかどうかは Decision Policy が決め、後続対応の候補は ActionCandidate として score から分けて扱う
* Review Queue と Action Worker は ActionCandidate を入力にする
* score_results、action_candidates、action_executions は、監査・再処理・評価への還流のために分けて履歴として残す
* 自動判定結果を teacher label に混ぜないことが、評価基盤と本番運用を接続するときの重要な境界になる
* Mermaid では四角だけを並べるより、`[(...)]` をデータ、`{{...}}` を判断、`[/.../]` を観察、角丸を処理として使うと、図だけでも責務の違いを追いやすい

## 次にやること

* Phase 8 として `docs/learning/design_map.md` を作り、`docs/design` の概念と既存実装の対応表を整理する
* その後、Phase 9 で ScoreResult / Decision Policy / ActionCandidate の最小実装に進む

## 疑問や保留

* Review Queue simulation は最初は CLI summary で十分か、notebook で観察する形にするかは Phase 11 の時点で決める
* local append-only log は JSONL から始め、Parquet や実 S3 は扱わない

---

## Phase 8 でやったこと

* `docs/learning/design_map.md` を作った
* 評価基盤設計と既存実装の対応表を整理した
* 運用スコアリング設計と Phase 9 以降で作る概念の対応表を整理した
* 評価基盤から運用スコアリング simulation へ進む Mermaid 図を追加した
* `score_results`、`action_candidates`、`action_executions` を分ける理由を説明した
* `docs/planning/tasks.md` の Phase 8 を完了にした

## Phase 8 で学んだこと

* 既存の `scored_rows` は評価作業の中間成果であり、運用側で追跡する `ScoreResult` とは目的が違う
* `ScoreResult` は scorer の出力履歴であり、action そのものではない
* `ActionCandidate` は Decision Policy の出力であり、score と後続対応候補を分けるために必要である
* `ActionExecution` は worker や reviewer の処理結果であり、candidate が作られた事実とは分けて残す
* Phase 9 では、scorer が直接 action しないことを dataclass と policy 関数の境界で表す

## 次にやること

* Phase 9 として `production_schema.py` を作り、`ScoreResult` / `DecisionResult` / `ActionCandidate` を定義する
* `decision_policy.py` で high score / low score / dry-run の toy policy を実装する
* production schema と decision policy のテストを追加する

---

## Phase 9 でやったこと

* `src/abuse_detection/production_schema.py` を作り、`ScoreResult` / `DecisionResult` / `ActionCandidate` を `dataclass` として定義した
* `ScoreResult` では `risk_score` が 0 から 100 の範囲にあることを検証するようにした
* `build_action_candidate` で、ScoreResult と DecisionResult から ActionCandidate を作れるようにした
* `src/abuse_detection/decision_policy.py` を作り、`DecisionPolicy` クラスを追加した
* `DecisionPolicy.decide` で `no_action` / `review_required` / `auto_action_candidate` を返すようにした
* `DecisionPolicy.build_candidate` で、`no_action` の場合は候補化せず、review / auto action の場合だけ ActionCandidate を作るようにした
* `tests/test_production_schema.py` と `tests/test_decision_policy.py` を追加した

## Phase 9 で学んだこと

* Python の `dataclass` は、Go の `struct` に近い感覚でデータ型を表現できる
* `frozen=True` にすると、ScoreResult のような append-only log 的なデータを扱いやすい
* scorer の出力である `ScoreResult` と、policy の出力である `ActionCandidate` を分けることで、scorer が直接 action しない境界をコードで表せる
* `DecisionPolicy` をクラスにすると、threshold や dry-run 設定を持つ policy object として読める

## 次にやること

* Phase 10 として local append-only log simulation に進む
* `score_results` と `action_candidates` を JSONL として `data_lake/` 配下に書き出す
* 書き込み完了を示す `_SUCCESS` または `manifest.json` 相当を作る
